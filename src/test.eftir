#def fn f {
#{f}:
#}

#def call f {
    push_frame #{'"' ++ f ++ '"'}
    ret_ip = ip + 2
    jmp :#{f}
#}

#def reg_handler id n {
    push_handler #{id} :#{n}
    jmp :#{n ++ "_scope"}
    pop_handler #{id}
#}

fn(main)
    push_frame "main"
    call(hello)
    ret void

fn(hello)
    print "hello, world!"
    ret void
